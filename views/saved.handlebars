
<ul>
  {{#each articles}}
 
     <li>

       <a href="{{link}}"><h2>{{title}}</h2></a>

       <button  data-id="{{_id}}" class="articles/:_id">Update Article!</button>
	   <button  data-id="{{_id}}" class="/article/:_id">Make a Note!</button>

     </li>
  
  {{/each}}
</ul>