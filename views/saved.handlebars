
<ul>
  {{#each articles}}
    {{#if saved}}
     <li>

       <a href="{{link}}"><h2>{{title}}</h2></a>

       <button  data-id="{{_id}}" class="unSaveArticleBtn">Unsave Article</button>
	   <button  data-id="{{_id}}" class="makeANote">Make a Note!</button>

     </li>
    {{/if}}
  {{/each}}
</ul>